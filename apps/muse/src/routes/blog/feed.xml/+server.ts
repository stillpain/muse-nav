import { listPosts } from '$lib/server/db';
const origin=process.env.ORIGIN||'https://musedaohang.com';const esc=(v:string)=>v.replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;');
export const GET=()=>{const posts=listPosts();const xml=`<?xml version="1.0" encoding="UTF-8"?><rss version="2.0"><channel><title>暮色手记</title><link>${origin}/blog</link><description>暮色导航的个人博客</description>${posts.map(p=>`<item><title>${esc(p.title)}</title><link>${origin}/blog/${encodeURIComponent(p.slug)}</link><guid>${origin}/blog/${encodeURIComponent(p.slug)}</guid><description>${esc(p.excerpt)}</description><pubDate>${new Date(p.publishedAt).toUTCString()}</pubDate></item>`).join('')}</channel></rss>`;return new Response(xml,{headers:{'content-type':'application/rss+xml; charset=utf-8'}})};
